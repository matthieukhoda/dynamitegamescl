{% extends 'base.html.twig' %}

{% block title %}Plateform
{% endblock %}

{% block body %}
	<h1>Console</h1>

	<table class="table">
		<tbody>
			{# <tr>
						                <th>Id</th>
						                <td>{{ plateform.id }}</td>
						            </tr> #}
			<tr>
				<th>Plateforme</th>
				<td>{{ plateform.plateforme }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('plateform_index') }}">Retour à la liste</a>

	<a href="{{ path('plateform_edit', {'id': plateform.id}) }}">Editer</a>

	{{ include('plateform/_delete_form.html.twig') }}
{% endblock %}
